# GOV-PHASE1-REMEDIATION-8 Implementation Plan

This L3 remediation addresses only the four filed A7 gaps. It keeps the current Candidate and Human Commit gates at NO-GO, steady-state at DISABLED, Migration 320 at NO-GO, and leaves formal acceptance to a separately authorized A8.

## Ordered work

1. Capture byte-size and SHA256 baselines for all named frozen Tasks, production governance sources, the Migration 320 Task, and five external evidence files.
2. Replace Gate calculation and saved projections with one allow-listed production Gate Router and a machine-readable dependency matrix.
3. Add production-path isolation cases and mutations for final approval, Session B, steady-state anchors, Migration 320 decisions, and output-only projections.
4. Replace the binary magic declaration with one exact nine-entry registry whose hash and per-entry oracles bind the scanner report and candidate record.
5. Repair the dedicated Railway Domain Review Schema, remove inline/fallback validation, and compile one manifest-bound governance schema set.
6. Route governance mechanism and external Migration 320 decisions to separate Gates and add negative schema and routing tests.
7. Rebuild the exact manifest, scanner report, and candidate record after source convergence.
8. Compare frozen baselines, verify Migration 320 remains VALID/NO-GO/exit 2, and stop before A8.

## Prohibited operations

No Git command, product read or write, service, product test, seed, migration, database operation, A8, Session B, or GO declaration is authorized.
