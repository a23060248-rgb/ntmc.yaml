# Phase 1.2 Review Remediation Plan

1. Separate governance commit artifacts from non-commit external evidence using machine-readable classifications.
2. Apply deterministic full scanning to governance commit candidates and limited, non-global claims to external evidence.
3. Give each fixture invocation a unique run root, record per-case execution metadata, and clean only that run root.
4. Run serialized and concurrent regression suites, revalidate Migration 320, and compare Session A artifacts to its recorded after-baseline.
5. Stop at `NO-GO`; a fresh A2 independent review requires a separate human instruction.
