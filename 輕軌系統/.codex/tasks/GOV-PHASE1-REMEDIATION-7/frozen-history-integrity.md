# Frozen History Integrity

The pre-change baseline records byte size and SHA256 for 340 frozen historical Task files. The post-change comparison must show zero missing, added, size-changed, or hash-changed frozen-history entries.

The comparison is independent of Git tracking state. A6 Reviewer outcomes remain byte-identical and archived as filed.
