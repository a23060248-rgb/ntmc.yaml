# Migration 320 Integrity

The pre-change baseline records 13 Migration 320 Task files and five external evidence files by byte size and SHA256. The post-change comparison must show no change in either set.

The production validator derives Migration 320 reasons only from its own Task artifacts. Proposed Rules, original evidence blockers, incomplete compatibility/domain decisions, and pending human execution approval keep Migration 320 structural VALID, gate NO-GO, exit 2. R7 does not reinterpret or modify original evidence.
