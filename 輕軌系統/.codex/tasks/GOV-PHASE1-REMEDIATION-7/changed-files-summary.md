# Changed Files Summary

Remediation 7 changes only bootstrap governance controls, schemas, production validators, production-path tests, lifecycle projection, and this local Task record. No product, database, migration, external evidence, or frozen historical Task file is changed.

The main source groups are:

- five-gate routing and target-gate exit semantics;
- typed Railway mechanism versus Migration 320 decision routing;
- scanner contract bundle, canonicalization pipeline, narrow binary oracle, report schema, report validator, generator, and candidate binding;
- schema-loader integration, lifecycle DISABLED projection, candidate manifest exclusion rules, fixtures, integration tests, scanner production cases, concurrency checks, and production mutations.

The exact source delta is machine-recorded by `baseline-comparison.json`: 24 modified, 11 added, and zero deleted governance-source files. The 35 paths are:

`AGENTS.md`; four modified schemas and two new schemas; six modified governance policy/status files and four new scanner contract inputs; nine modified scripts and three new production modules; four modified test sources and two new production scanner test sources. The detailed path-level before/after hashes are in the comparison artifact. This report does not rely on Git state.
