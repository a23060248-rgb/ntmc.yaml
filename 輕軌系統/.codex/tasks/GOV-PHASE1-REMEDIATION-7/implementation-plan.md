# GOV-PHASE1-REMEDIATION-7 Implementation Plan

This L3 remediation closes the three concrete A6 trust-boundary defects. It keeps the current candidate at NO-GO, makes steady-state unavailable as DISABLED, and leaves formal acceptance to a future independently authorized A7.

## Ordered work

1. Capture SHA256 and byte-size baseline for governance sources, named frozen Tasks, Migration 320 Task artifacts, and five external evidence files.
2. Replace the shared calculated gate with one production Gate Router and target-specific exit semantics.
3. Separate Railway governance-mechanism outcome from Migration 320 external business decision.
4. Replace the flat scanner report with a schema-validated, manifest-bound, contract-bundle-bound, self-hashed report.
5. Narrow the binary oracle to five finding classes and nine signatures; remove extension-magic mismatch.
6. Add production-path Gate, scanner, safe-negative, schema, reference, and mutation tests.
7. Rebuild the exact candidate manifest, scanner report, and candidate record; validate current Task and Migration 320.
8. Recompute the immutable baseline and stop before A7 and Session B.

## Prohibited operations

No Git command, product read or write, service, product test, seed, migration, database operation, A7, Session B, or GO declaration is authorized.
