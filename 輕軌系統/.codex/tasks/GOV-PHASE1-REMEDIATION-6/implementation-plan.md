# GOV-PHASE1-REMEDIATION-6 Implementation Plan

This L3 remediation implements the human-selected Bootstrap convergence option. It does not create trust from an uncommitted validator; it limits machine evidence to deterministic internal consistency and leaves commit authorization to independent Review and the human owner.

## Ordered work

1. Preserve baseline-before for governance sources, all prior Task history, and Migration 320 artifacts.
2. Replace the three-stage and steady-state proof claims with four explicit NO-GO Gates and a bootstrap trust policy.
3. Make the six-operand scope lattice actor-bound for every production consumer.
4. Replace WORKSPACE_CANDIDATE/STAGED_EXACT with BOOTSTRAP_WORKSPACE_CANDIDATE and INTERNAL_CONSISTENCY_ONLY assurance; remove the staged executable path.
5. Converge scanner canonicalization and typed Rule authority, downgrade lifecycle to informational-only, and rebuild the exact candidate collector.
6. Execute production-path property, mutation, collision, canonicalization, scanner, schema-loader, manifest, history, and Migration 320 validation.
7. Record all four Gates as NO-GO and stop before Session A6.

## Prohibited operations

No Git command, product read or write, service, product test, seed, migration, database operation, Session A6, Session B, or GO declaration is authorized.
